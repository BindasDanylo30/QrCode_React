name: Check
on: push
jobs:
 lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v2
      - name: install dependencies
        run: npm ci
      - name: Lint 
        run: npm run Lint
      